/* KELO-INDEX
 * area: TEST / MAP FORGE / STUDIO HANDOFF
 * owner: Map Forge Studio handoff contract audit
 * purpose: prove deterministic city projection into an authority-compatible editable draft without direct LIVE mutation
 * online: validates the replaceable KELO_WORLD_EDIT import boundary
 */
import fs from'node:fs';import assert from'node:assert/strict';
import{generateBestOf}from'../src/world/map-forge/map-forge-core.mjs';import{getMapForgeRecipe}from'../src/world/map-forge/map-forge-recipes.mjs';import{mapDefinitionToWorldDraftSnapshot,mapForgeDocumentMetadata}from'../src/studio/adapters/map-forge-draft-importer.mjs';
const recipe=getMapForgeRecipe('KELO_ROYAL_CAPITAL_V1'),run=generateBestOf(recipe,{seed:48291,count:4}),map=run.best;assert(map?.validation?.valid);const a=mapDefinitionToWorldDraftSnapshot(map),b=mapDefinitionToWorldDraftSnapshot(map);assert.deepEqual(a,b,'projection must be deterministic');const rows=Object.values(a.cells);assert(rows.length>5000,'city draft should contain real terrain cells');assert(rows.some(x=>x.role==='path'),'generated roads must become editable path cells');assert(rows.every(x=>x.material==='grass'||x.material==='marble'),'projection must emit only current runtime materials');assert.equal(a.worldId,'world:kelo-main');const meta=mapForgeDocumentMetadata(map);assert(meta.tags.includes('map-forge')&&meta.tags.some(x=>x.startsWith('seed:')));const importer=fs.readFileSync('src/studio/adapters/map-forge-draft-importer.mjs','utf8'),world=fs.readFileSync('src/creators/workspaces/world-workspace.mjs','utf8'),entry=fs.readFileSync('src/creators/creator-entry.mjs','utf8'),hub=fs.readFileSync('src/creators/ui/creator-hub.mjs','utf8'),ui=fs.readFileSync('src/creators/ui/map-forge-workspace.mjs','utf8');assert(importer.includes("world:draft:create")&&importer.includes('forceNew:true')&&importer.includes("world:draft:import"));for(const bad of['KELO_COLLISION.replaceOwner','KELO_WORLD_RENDERER=','KELO_PROPERTY_SYSTEM.request'])assert(!importer.includes(bad),`direct LIVE mutation forbidden: ${bad}`);assert(world.includes('importMapForgeIntoWorldDraft')&&world.includes('openKeloStudioLive'));assert(entry.includes('registerMapForgeWorkspace')&&entry.includes('openWorkspace});'));assert(hub.includes("['map-forge','Map Forge','active']"));assert(ui.includes('ABRIR EN WORLD EDITOR')&&ui.includes('createMapForgeWorkerClient'));console.log(JSON.stringify({ok:true,seed:map.metadata.seed,score:map.quality.total,cells:rows.length,paths:rows.filter(x=>x.role==='path').length,placements:a.placements.length,layoutHash:map.metadata.layoutHash},null,2));
