#!/usr/bin/env node
'use strict';
const fs=require('fs');const path=require('path');const vm=require('vm');const assert=require('assert');
const ROOT=path.resolve(__dirname,'..');
function load(sandbox,file){const source=fs.readFileSync(path.join(ROOT,file),'utf8');new vm.Script(source,{filename:file}).runInContext(sandbox);}
function makeSandbox(extra){
  const listeners=new Map(),movementHooks=[],renderHooks=[];
  const s={console,Date,Math,JSON,Map,Set,Object,Array,Number,String,Boolean,Promise,Infinity,NaN,parseInt,parseFloat,isFinite,setTimeout,clearTimeout,performance:{now:()=>Date.now()},STATE:{gold:100000,kc:0,inventory:[],equipped:[]},localPlayer:{id:'local_pioneer',name:'Audit',x:100,y:100,vx:0,vy:0,hp:100,maxHp:100,radius:20},simulatedPlayers:[],input:{normX:0,normY:0},saveCount:0,saveState(){this.saveCount++;},KeloBackpack:{ensure(){if(!s.STATE.backpack)s.STATE.backpack={capacity:20,slots:new Array(20).fill(null)};}},KeloEquipment:{isEquipped(){return false;}},KeloEvents:{on(name,fn){if(!listeners.has(name))listeners.set(name,new Set());listeners.get(name).add(fn);return()=>listeners.get(name).delete(fn);},emit(name,payload){for(const fn of listeners.get(name)||[])fn(payload);}},KeloMovement:{after(owner,fn){movementHooks.push({owner,fn});return owner;}},KeloRender:{afterFrame(owner,fn){renderHooks.push({owner,fn});return owner;}},KeloCamera:{worldToScreen(x,y){return{x,y};},getEffectiveZoom(){return 1;}},ctx:{save(){},restore(){},translate(){},scale(){},fillRect(){},strokeRect(){},beginPath(){},arc(){},fill(){},stroke(){},moveTo(){},lineTo(){},set fillStyle(v){},set strokeStyle(v){},set lineWidth(v){}},innerWidth:390,innerHeight:844};
  Object.assign(s,extra||{});s.window=s;s.globalThis=s;return vm.createContext(s);
}
function approx(a,b,tol,msg){assert(Math.abs(a-b)<=tol,(msg||'values differ')+`: ${a} vs ${b}`);}
async function main(){
  const s=makeSandbox();
  load(s,'src/systems/regional-economy-math.js');load(s,'src/systems/container-system.js');load(s,'src/systems/regional-economy-system.js');load(s,'src/systems/faction-clan-system.js');load(s,'src/systems/caravan-system.js');
  const E=s.KeloRegionalEconomy,C=s.KeloContainers,F=s.KeloFactions,V=s.KeloCaravans,M=s.KeloRegionalEconomyMath;
  assert(E&&C&&F&&V&&M,'foundation owners must load');

  // Pure math: objective risk changes economic distance and attenuates demand.
  const easy={travelTime:15,pvpExposure:.1,terrainDanger:.1,chokePointRisk:.1,npcThreat:.1,eventRisk:.1},danger={travelTime:15,pvpExposure:.9,terrainDanger:.8,chokePointRisk:.8,npcThreat:.9,eventRisk:.8};
  assert(M.riskBreakdown(danger).score>M.riskBreakdown(easy).score,'danger risk score must be higher');
  assert(M.economicEdgeCost(danger)>M.economicEdgeCost(easy),'risk must increase economic edge cost');
  assert(M.propagatedDemand(30,M.economicEdgeCost(danger),30)<M.propagatedDemand(30,M.economicEdgeCost(easy),30),'risk must reduce propagated demand');
  const graph=[{from:'A',to:'B',travelTime:10,pvpExposure:.05},{from:'A',to:'C',travelTime:4,pvpExposure:1},{from:'C',to:'B',travelTime:40,pvpExposure:.1}];
  const pathResult=M.shortestEconomicPath(graph,'A','B',{riskMultiplier:3});assert.deepStrictEqual(Array.from(pathResult.path),['A','B'],'economic routing must not reward deliberately longer path');

  // Lazy production and specialization.
  E.dev.setStock('ignis','coal',.50,'T1');const coalBefore=E.quote('ignis','coal','T1',{skipRefresh:true}).stock;const prod=E.dev.advanceHours('ignis',1);assert(prod.ok&&prod.produced.coal>499,'one elapsed hour must produce ~500 coal');approx(E.quote('ignis','coal','T1',{skipRefresh:true}).stock-coalBefore,500,1.5,'coal production');
  assert(E.productionProfile('ignis','coal').hourlyRate>E.productionProfile('verdantia','coal').hourlyRate,'specialist must produce more');assert(E.productionProfile('ignis','coal').qualityDistribution.T3>E.productionProfile('verdantia','coal').qualityDistribution.T3,'specialist must produce better tiers');

  // Scarcity price is stock-derived and explainable.
  E.dev.setStock('verdantia','stone',.90,'T1');const abundant=E.quote('verdantia','stone','T1',{skipRefresh:true});E.dev.setStock('verdantia','stone',.30,'T1');const scarce=E.quote('verdantia','stone','T1',{skipRefresh:true});assert(scarce.unitPrice>abundant.unitPrice,'scarcity must raise price');assert(Number.isFinite(scarce.breakdown.base)&&Number.isFinite(scarce.breakdown.scarcity)&&Number.isFinite(scarce.breakdown.regionalDemand),'quote must expose breakdown');

  // Buy reduces stock; sell increases stock; inventory uses shared container owner.
  E.dev.setStock('verdantia','apple',.70,'T1');let before=E.quote('verdantia','apple','T1',{skipRefresh:true}).stock;const buy=await E.request('BuyResource',{settlementId:'verdantia',resourceId:'apple',tier:'T1',quantity:10});assert(buy.ok,'buy resource must succeed');let afterBuy=E.quote('verdantia','apple','T1',{skipRefresh:true}).stock;approx(before-afterBuy,10,.01,'buy stock delta');let appleSlot=C.getSlots('backpack').find(x=>x.item&&x.item.resourceId==='apple');assert(appleSlot,'bought resource must exist in Backpack');const sell=await E.request('SellResource',{settlementId:'verdantia',itemKey:appleSlot.key,quantity:5});assert(sell.ok,'sell resource must succeed');approx(E.quote('verdantia','apple','T1',{skipRefresh:true}).stock-afterBuy,5,.01,'sell stock delta');

  // Emergency contract, real delivery, decreasing reward, recovery closes it.
  const emergency=E.dev.setStock('ignis','apple',.08,'T1');assert(emergency.contract&&emergency.contract.status==='ACTIVE','critical reserve must create supply contract');const contractId=emergency.contract.id,rewardBefore=emergency.contract.currentReward,stockBeforeDelivery=E.quote('ignis','apple','T1',{skipRefresh:true}).stock;
  const received=C.receiveItem('backpack',{id:'audit_supply_apples',templateId:'resource_apple',kind:'resource',resourceId:'apple',tier:'T1',quantity:100,maxStack:200,weight:.35},{persist:false,preserveIdentity:true});assert(received.ok,'supply fixture must enter Backpack through container owner');const delivery=await E.request('DeliverSupplyContract',{contractId,itemKey:received.itemKey,quantity:100});assert(delivery.ok,'contract delivery must succeed');assert(delivery.contract.remainingQuantity<emergency.contract.remainingQuantity,'delivery must reduce remaining quantity');assert(delivery.stock>stockBeforeDelivery,'delivery must increase real reserve');assert(delivery.contract.currentReward<rewardBefore,'reward must decline as shortage recovers');E.dev.setStock('ignis','apple',.50,'T1');assert(E.getContract(contractId).status==='COMPLETED','recovered reserve must complete contract');

  // Reset emergency then verify risk -> distance -> less propagation -> bigger margin.
  const emergency2=E.dev.setStock('ignis','apple',.08,'T1').contract;E.dev.setStock('verdantia','apple',.70,'T1');E.dev.setStock('ferrum','apple',.70,'T1');const easyDistance=E.getEconomicDistance('verdantia','ignis'),dangerDistance=E.getEconomicDistance('ferrum','ignis');assert(dangerDistance.distance>easyDistance.distance,'dangerous origin must be economically farther');const easyQuote=E.quote('verdantia','apple','T1',{skipRefresh:true}),dangerQuote=E.quote('ferrum','apple','T1',{skipRefresh:true});assert(easyQuote.breakdown.regionalDemand>dangerQuote.breakdown.regionalDemand,'easy market must receive more demand pressure');const destReward=emergency2.currentReward;assert(destReward-dangerQuote.unitPrice>destReward-easyQuote.unitPrice,'dangerous route must create larger potential margin without bonus');
  const riskBefore=E.getRouteRisk('route_ignis_verdantia').score,priceBeforeRaid=easyQuote.unitPrice;const raid=await E.request('ActivateRaiderBand',{routeId:'route_ignis_verdantia',ambushNodeId:'forest_iv',npcThreat:.20,eventRisk:.40});assert(raid.ok&&raid.risk.score>riskBefore,'RaiderBand must increase route risk');const duringRaid=E.quote('verdantia','apple','T1',{skipRefresh:true});assert(duringRaid.unitPrice<priceBeforeRaid,'increased route risk must compress propagated source price');await E.request('EndWorldEvent',{eventId:raid.event.id});approx(E.getRouteRisk('route_ignis_verdantia').score,riskBefore,.000001,'risk must restore after event');

  // Cart is physical/persistent, cargo shares container, attached carrier cannot attack, dropped cart can be stolen, cargo is hidden.
  const resourceSlot=C.getSlots('backpack').find(x=>x.item&&x.item.kind==='resource');assert(resourceSlot,'resource fixture for cart required');let attach=await V.request('AttachCart',{cartId:'cart_demo_1',actorId:'local_pioneer'});assert(attach.ok&&attach.cart.state==='ATTACHED','cart must attach');assert.strictEqual(V.canActorAttack('local_pioneer'),false,'attached carrier cannot attack');assert.strictEqual((await V.request('BeginClaimCart',{cartId:'cart_demo_1',actorId:'enemy'})).error,'CART_ATTACHED_PROTECTED','attached cart cannot be stolen directly');const loadCart=await V.request('LoadCart',{cartId:'cart_demo_1',actorId:'local_pioneer',itemKey:resourceSlot.key,quantity:1});assert(loadCart.ok&&V.cartWeight('cart_demo_1')>0,'cart must use real container inventory');const hidden=V.inspectCart('cart_demo_1','enemy');assert.strictEqual(hidden.cargoVisible,false,'enemy cargo must remain hidden');assert.strictEqual(hidden.cargo,'UNKNOWN','enemy sees unknown cargo');const own=V.inspectCart('cart_demo_1','local_pioneer');assert(own.cargoVisible&&own.cargo.length>0,'authorized owner can inspect cargo');let drop=await V.request('DetachCart',{cartId:'cart_demo_1',actorId:'local_pioneer'});assert(drop.ok&&drop.cart.state==='DROPPED','voluntary detach must drop cart');let claim=await V.request('BeginClaimCart',{cartId:'cart_demo_1',actorId:'enemy'});assert(claim.ok&&claim.cart.state==='CLAIMING','dropped cart must be claimable');const stolen=V.dev.finishClaim('cart_demo_1','enemy');assert(stolen.ok&&stolen.cart.currentControllerId==='enemy','claim must transfer controller, not owner');assert.strictEqual(stolen.cart.owner.id,'local_pioneer','robbery must preserve owner identity separately');const deathDrop=V.dev.killCarrier('cart_demo_1');assert(deathDrop.ok&&deathDrop.cart.state==='DROPPED','carrier death must abandon cart in world');const persisted=JSON.parse(JSON.stringify(s.STATE));s.STATE=persisted;assert(V.getCart('cart_demo_1')&&C.getContainer('cart_inv_cart_demo_1'),'cart and its container must survive state serialization/reload');

  // Faction/clan foundation and permissions.
  const joinFaction=await F.request('JoinFaction',{playerId:'local_pioneer',factionId:'solari'});assert(joinFaction.ok,'player must join faction');const clan=await F.request('CreateClan',{playerId:'local_pioneer',name:'Audit Clan',tag:'AUD'});assert(clan.ok&&clan.clan.factionId==='solari','clan must bind to faction');assert(F.hasPermission(clan.clan.id,'local_pioneer','MANAGE_CARAVANS'),'Leader permissions must be data-driven');const owner=F.normalizeOwner({type:'CLAN',id:clan.clan.id});assert(owner&&owner.type==='CLAN','ownership model must support clan');

  // Combat owner enforces attached cart lock while still allowing damage against carrier.
  await V.request('AttachCart',{cartId:'cart_demo_1',actorId:'local_pioneer'});s.KeloCombatSchema={events:{ATTACK_STARTED:'a',ATTACK_RESOLVED:'b',HIT_CONFIRMED:'c',DAMAGE_APPLIED:'combat:damage_applied',ENTITY_KILLED:'combat:entity_killed'}};s.KeloHitResolver={withinRange(){return{hit:true,distance:1,range:10};}};s.KeloDamageResolver={apply(target,amount){target.hp-=amount;return{amount,requested:amount,absorbed:0,hp:target.hp,killed:target.hp<=0};}};load(s,'src/systems/combat/combat-engine.js');const target={id:'enemy_target',x:101,y:100,hp:100};const blocked=s.KeloCombatEngine.attack({attacker:s.localPlayer,target,profile:{range:10,damage:10,cooldown:1}});assert.strictEqual(blocked.reason,'ACTION_BLOCKED_BY_CART','combat must reject carrier attack');s.KeloEvents.emit('combat:damage_applied',{targetActorId:'local_pioneer',target:s.localPlayer,amount:5});assert(V.getCart('cart_demo_1').state==='ATTACHED','carrier can receive damage without losing attached cart');s.KeloEvents.emit('combat:entity_killed',{targetActorId:'local_pioneer',target:s.localPlayer});assert(V.getCart('cart_demo_1').state==='DROPPED','carrier death event must release cart');

  const audit=C.auditIdentities();assert(audit.ok,'one item identity must belong to one container');
  console.log(JSON.stringify({ok:true,version:E.version,checks:{production:true,stockTransactions:true,scarcity:true,contracts:true,riskMargin:true,raiders:true,carts:true,hiddenCargo:true,factions:true,clans:true,persistence:true,combatCartLock:true},easyDistance:easyDistance.distance,dangerDistance:dangerDistance.distance,riskBefore,raiderRisk:raid.risk.score},null,2));
}
main().catch(err=>{console.error(err&&err.stack||err);process.exit(1);});
