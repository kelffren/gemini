'use strict';
const fs=require('fs'),vm=require('vm'),assert=require('assert');
let saveCalls=0;
const context={console,Date,Math,Map,Set,Object,Array,Number,String,JSON,Promise,setTimeout,clearTimeout,STATE:{inventory:[],equipmentSlots:{}},localPlayer:{},saveState(){saveCalls++;},window:{}};context.window=context;vm.createContext(context);
for(const file of ['src/systems/equipment-system.js','src/systems/backpack-system.js','src/systems/container-system.js'])vm.runInContext(fs.readFileSync(file,'utf8'),context,{filename:file});
const E=context.KeloEquipment,B=context.KeloBackpack,C=context.KeloContainers;assert(E&&B&&C);assert.equal(E.version,'equipment-v1.1.2');assert.equal(B.version,'backpack-v1.1.0');assert.equal(C.version,'container-v1.1.0');
const totalQty=()=>context.STATE.inventory.concat(context.STATE.warehouse.items).reduce((n,x)=>n+Math.max(1,Number(x.quantity)||1),0);
const keys=()=>context.STATE.inventory.concat(context.STATE.warehouse.items).map((x,i)=>C.keyForItem(x,i));
const assertUnique=()=>assert.equal(new Set(keys()).size,keys().length,'identity must be unique across containers');
function bagSlotById(id){return C.getSlots('backpack').find(s=>s.item&&s.item.id===id);}function whSlotById(id){return C.getSlots('warehouse').find(s=>s.item&&s.item.id===id);}
assert.equal(C.getStats('warehouse').capacity,30);
const relic={id:'relic_1',templateId:'relic',name:'Reliquia',kind:'material',quantity:1,maxStack:1,bound:true,rarity:'Epic',metadata:{origin:'audit'}};context.STATE.inventory.push(relic);B.ensure();let before=totalQty(),slot=bagSlotById('relic_1');let r=C.transferItem('backpack','warehouse',slot.key);assert(r.ok&&r.preservedIdentity);assert(!bagSlotById('relic_1')&&whSlotById('relic_1'));assert.strictEqual(context.STATE.warehouse.items.find(x=>x.id==='relic_1'),relic);assert.equal(relic.bound,true);assert.equal(relic.rarity,'Epic');assert.equal(relic.metadata.origin,'audit');assert.equal(totalQty(),before);assertUnique();
r=C.transferItem('warehouse','backpack',whSlotById('relic_1').key);assert(r.ok&&r.preservedIdentity);assert(bagSlotById('relic_1'));assert.equal(totalQty(),before);
const partial={id:'ore_source',templateId:'ore_a',name:'Mineral',kind:'material',quantity:8,maxStack:10,rarity:'Rare'};context.STATE.inventory.push(partial);B.ensure();before=totalQty();slot=bagSlotById('ore_source');r=C.transferItem('backpack','warehouse',slot.key,3);assert(r.ok);assert.equal(partial.quantity,5);const moved=context.STATE.warehouse.items.find(x=>x.splitFrom==='ore_source');assert(moved&&moved.quantity===3&&moved.id!=='ore_source');assert.equal(totalQty(),before);assertUnique();
const dst={id:'ore_dst',templateId:'ore_b',name:'Cristal',kind:'material',quantity:6,maxStack:10};context.STATE.warehouse.items.push(dst);C.ensure();const src={id:'ore_src',templateId:'ore_b',name:'Cristal',kind:'material',quantity:7,maxStack:10};context.STATE.inventory.push(src);B.ensure();before=totalQty();r=C.transferItem('backpack','warehouse',bagSlotById('ore_src').key,7);assert(r.ok);assert.equal(dst.quantity,10,'first compatible stack must fill to maxStack');const overflow=context.STATE.warehouse.items.find(x=>x.id==='ore_src');assert(overflow&&overflow.quantity===3,'overflow must remain as moved source identity');assert.equal(totalQty(),before);assertUnique();
const equipped=context.STATE.inventory.find(x=>x&&x.id==='eq_weapon');r=C.transferItem('backpack','warehouse',bagSlotById('eq_weapon').key);assert.equal(r.error,'EQUIPPED_ITEM_PROTECTED');E.unequipItem('weapon');r=C.transferItem('backpack','warehouse',bagSlotById('eq_weapon').key);assert(r.ok,'unequipped equipment should store');assert(!context.STATE.inventory.some(x=>x.id==='eq_weapon'));assert(context.STATE.warehouse.items.some(x=>x.id==='eq_weapon'));E.getEquipped();assert.equal(context.STATE.inventory.filter(x=>x.id==='eq_weapon').length,0,'equipment ensure must not duplicate stored starter');assert.strictEqual(context.STATE.equipmentSlots.weapon,null);assert.equal(E.equipItem('eq_weapon').error,'ITEM_NOT_FOUND','stored equipment cannot equip');r=C.transferItem('warehouse','backpack',whSlotById('eq_weapon').key);assert(r.ok);assert(E.equipItem('eq_weapon').ok);
const snapshot=JSON.stringify({inventory:context.STATE.inventory,backpack:context.STATE.backpack,warehouse:context.STATE.warehouse});context.STATE=JSON.parse(snapshot);C.ensure();assert(context.STATE.warehouse&&Array.isArray(context.STATE.warehouse.items),'reload-shaped state must preserve warehouse');assertUnique();
const fillTemplate=i=>({id:'fill_'+i,templateId:'fill_'+i,name:'Filler',kind:'item',quantity:1,maxStack:1});context.STATE.warehouse.items=[];context.STATE.warehouse.slots=new Array(context.STATE.warehouse.capacity).fill(null);for(let i=0;i<context.STATE.warehouse.capacity;i++){const x=fillTemplate(i);context.STATE.warehouse.items.push(x);context.STATE.warehouse.slots[i]=C.keyForItem(x,i);}const blocked={id:'blocked',templateId:'blocked',name:'Blocked',kind:'item',quantity:1,maxStack:1};context.STATE.inventory.push(blocked);B.ensure();const beforeState=JSON.stringify({inventory:context.STATE.inventory,backpack:context.STATE.backpack,warehouse:context.STATE.warehouse});r=C.transferItem('backpack','warehouse',bagSlotById('blocked').key);assert.equal(r.error,'DESTINATION_FULL');assert.equal(JSON.stringify({inventory:context.STATE.inventory,backpack:context.STATE.backpack,warehouse:context.STATE.warehouse}),beforeState,'full destination must have zero mutation');
assert.equal(C.transferItem('bogus','warehouse','x').error,'INVALID_CONTAINER');assert.equal(C.transferItem('backpack','warehouse','missing').error,'ITEM_NOT_FOUND');assert.equal(C.transferItem('backpack','warehouse',bagSlotById('blocked').key,0).error,'INVALID_AMOUNT');assertUnique();assert(saveCalls>0);
console.log('PASS container-system-audit',JSON.stringify({version:C.version,warehouseCapacity:C.warehouseCapacity,strictAtomic:true,identityUnique:true,saves:saveCalls}));