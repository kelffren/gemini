# KELO WORLD — host-agnostic Netlify adapter
# GitHub remains the source of truth. Netlify is the primary static CDN target;
# GitHub Pages remains a fallback until another compatible edge target is enabled.
# Compression policy: Netlify CDN negotiates Brotli/Gzip automatically; this is
# NEVER accepted from configuration/comment alone and is live-tested by
# scripts/turbo-live-host-audit.mjs before the Turbo Guardian may pass.

[build]
  command = "node scripts/netlify-sync-pixelorama-runtime.mjs && npm install --no-save --ignore-scripts esbuild@0.25.9 && npm run build"
  publish = "."

# Never let control-plane documents become stale.
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache, max-age=0, must-revalidate"

[[headers]]
  for = "/version.json"
  [headers.values]
    Cache-Control = "no-cache, max-age=0, must-revalidate"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache, max-age=0, must-revalidate"

[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Cache-Control = "no-cache, max-age=0, must-revalidate"

[[headers]]
  for = "/dist/turbo/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Pixelorama runtime control plane: always resolve the current immutable build.
[[headers]]
  for = "/tools/pixelorama/runtime/current.json"
  [headers.values]
    Cache-Control = "no-cache, max-age=0, must-revalidate"
    Access-Control-Allow-Origin = "*"
    Cross-Origin-Resource-Policy = "cross-origin"

# Pixelorama heavy binaries are content-versioned by the orphan runtime commit.
[[headers]]
  for = "/tools/pixelorama/runtime/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
    Access-Control-Allow-Origin = "*"
    Cross-Origin-Resource-Policy = "cross-origin"

# Security baseline for the static game surface.
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    X-Frame-Options = "SAMEORIGIN"
