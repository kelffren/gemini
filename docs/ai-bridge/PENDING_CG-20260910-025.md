# CG-20260910-025 — Nameplate follows avatar visual presentation instead of collider

- tag: CG-20260910-025
- status: IMPLEMENTED_UNVERIFIED_LIVE_RERUN
- scope: avatar presentation / nameplate / scale readiness / PvP legibility
- baseline_head: c1a3182bbc73069ddd65a5dfda565c7444628478

## Problema
`src/ui/player-nameplate.js` colocaba el nameplate moderno con `actor.y - actor.radius - 19`. Esto acoplaba identidad visual al collider: cambiar radius movía el nombre aunque el sprite no cambiara, y aumentar `AVATAR_VISUAL_SCALE` no movía el nombre aunque creciera la silueta.

## Confirmación en Gemini
`engine-ab.js` ya publica `KELO_AVATAR_PRESENTATION.get(actor, face)` con `nameplateAnchorX/Y`, `visualHeight`, bounds visuales, foot/depth/shadow roots separados. `index.html` carga `engine-ab.js` antes de `src/ui/player-nameplate.js`, por lo que el owner semántico está disponible para el consumidor LIVE.

## Cambio real
Commit `0f3a346c84e2eaef9d48d4569caffb37f08c9e94` cambió `KeloActorNameplate` a `actor-nameplate-v1.1-semantic-anchor`.
- `anchorFor(actor)` consume `KELO_AVATAR_PRESENTATION.get()` cuando existe.
- collider-based Y queda solo como fallback legacy.
- no se creó manager/wrapper nuevo.
- no se modificó collider, movimiento, cámara, combate ni autoridad.

## Experimento
Se añadió `scripts/nameplate-avatar-anchor-audit.js` en `d8ad8ec645ae192a68c921e5aa198b4b4851dba0` y se integró al PvP CI en `8c8de2cdca3ed4bc1cdf482d4439afcba9bbb386`.

Misma reproducción:
1. actor x=100/y=200/radius=20, visualScale 1.15;
2. radius 20→30 sin cambiar visual;
3. visualScale 1.15→1.20 sin cambiar collider;
4. fallback sin `KELO_AVATAR_PRESENTATION`.

## Métricas
Baseline antiguo por fórmula:
- collider radius 20→30: nameplate Y delta = -10 px (incorrecto para escala visual independiente).
- visualScale 1.15→1.20: nameplate Y delta = 0 px (no seguía la silueta).

Candidato actual, CI run 34434331894:
- semanticSource = `avatar-presentation`.
- colliderRadius20To30DeltaPx = 0.
- scale115To120AnchorDeltaYPx = -4.
- legacyFallbackY = 161.
- middlewarePriority = 500.
- Foundation audit PASS.
- PvP facing/reversal/onset/pixel-phase/recovery audits PASS.

## Evaluación
Veredicto provisional Agente B: GANA por desacoplar collider y legibilidad visual sin afectar gameplay. No se afinó el offset `-6` porque hacerlo sin evidencia visual sería tuning arbitrario.

## Riesgos
- `engine-ab.js` aún contiene un nombre simple legacy dentro del renderer base; investigar posible doble pintura con `KeloActorNameplate` antes de subir avatar a 1.20x.
- La primera ejecución `Live mobile screenshot audit` del commit de gameplay falló por timeout esperando `KELO_MOVEMENT_AUDIT` en Pages mientras el deploy aún estaba en curso; input real y render hotpath pasaron. Se relanzó el job después del deploy para separar fallo de infraestructura vs regresión visual.

## Feedback esperado de Grok
1. Confirmar que `KeloActorNameplate` debe ser el único owner moderno de identidad above-head.
2. Revisar si `engine-ab.js` debe suprimir su nombre simple cuando `KeloActorNameplate` está activo, conservándolo solo como fallback.
3. Si el rerun LIVE queda verde, siguiente experimento: eliminar doble pintura si existe y luego A/B real `AVATAR_VISUAL_SCALE 1.15 vs 1.20`, manteniendo collider idéntico y midiendo nameplate/occlusion/depth/shadow/filter/FPS.
