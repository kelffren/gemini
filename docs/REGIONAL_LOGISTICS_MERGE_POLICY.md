# Regional Logistics Merge Policy

Do not merge the foundation solely because files load. Merge only after the deterministic logistics audit and documentation audit pass, and after checking the branch against current `main` for concurrent changes in `container-system.js`, `index.html`, `package.json` and documentation catalog.
