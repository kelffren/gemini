# Scene Kit V1

**Actualizado:** 2026-09-23

Recetas de escena sobre Forest Plaza. No es un catálogo paralelo.

`SOURCE / PROPERTY CATALOG → SCENE KIT RECIPE → SESSION OVERLAY + LOCAL DRAFT`

- Preview en mundo vía `KELO_SCENE_KIT_RUNTIME` leído por `KELO_GENERIC_PROPS`.
- Draft en `localStorage` (`kelo.sceneKit.draft.v1`).
- Colliders `none`. No publica a World authority.
- Pack V1: solo `forest-plaza-v2`. Providers externos entran después por el mismo slot map.

Recetas: `plaza-noble`, `market-street`, `enclosed-garden`.

Creators → CONTENT → Scene Kit.
