(function(){'use strict';
const VERSION='pvp-world-v1.2',TRANSITION_MS=1150,WORLD=Object.freeze({x:2660,y:360,w:720,h:720,spawnX:2790,spawnY:720,dummyX:3190,dummyY:720});
const state={mode:'social',combatEnabled:false,transitioning:false,selected:null,armedSlot:-1,basicCooldown:0,saved:null,floats:[],commandSeq:1,resultSeq:1,dummy:null,transitionTimer:null,transitionStartedAt:0,transitionKind:null,lastError:null};
window.KELO_COMBAT_ENABLED=false;
function toast(m){if(typeof showToast==='function')showToast(m)}function clamp(n,a,b){return Math.max(a,Math.min(b,n))}function dist(a,b){return Math.hypot((a.x||0)-(b.x||0),(a.y||0)-(b.y||0))}function ready(){return typeof localPlayer!=='undefined'&&localPlayer&&typeof camera!=='undefined'&&camera}function dummyEntity(){if(state.dummy)return state.dummy;if(typeof simulatedPlayers==='undefined'||!simulatedPlayers||!simulatedPlayers.length)return null;return state.dummy=simulatedPlayers[0]}function screenWorld(x,y){if(typeof screenToWorld==='function')return screenToWorld(x,y);const z=(typeof CONFIG!=='undefined'&&CONFIG.zoom)||1;return{x:camera.x+(x-screenW/2)/z,y:camera.y+(y-screenH/2)/z}}function command(type,payload){return Object.freeze({id:state.commandSeq++,type,payload:payload||{},issuedAt:performance.now()})}
const localAuthority=Object.freeze({execute(c){if(!state.combatEnabled||state.transitioning)return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:false,reason:'COMBAT_DISABLED'});if(c.type==='SELECT_TARGET'){const t=c.payload.target;state.selected=t&&t.hp>0?t:null;return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:!!state.selected,type:'TARGET_SELECTED',targetId:state.selected&&state.selected.id})}if(c.type==='BASIC_ATTACK'){const t=c.payload.target;if(!t||t.hp<=0)return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:false,reason:'INVALID_TARGET'});if(state.basicCooldown>0)return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:false,reason:'COOLDOWN'});if(dist(localPlayer,t)>150)return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:false,reason:'OUT_OF_RANGE'});t.hp=Math.max(0,(t.hp==null?100:t.hp)-18);state.basicCooldown=.7;return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:true,type:'DAMAGE',targetId:t.id,amount:18,hp:t.hp})}if(c.type==='CAST_ABILITY'){if(!window.KeloAbilities||!window.KeloAbilities.engine)return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:false,reason:'ABILITY_ENGINE_UNAVAILABLE'});const r=window.KeloAbilities.engine.cast(c.payload.request);return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:!!r.valid,type:'ABILITY_RESULT',result:r})}return Object.freeze({id:state.resultSeq++,commandId:c.id,ok:false,reason:'UNKNOWN_COMMAND'})}});
function present(r,t){if(!r)return;if(r.ok&&r.type==='DAMAGE'&&t){state.floats.push({x:t.x,y:t.y-42,text:'-'+r.amount,life:.8});if(t.hp<=0)toast('Dummy derrotado')}else if(!r.ok&&r.reason==='OUT_OF_RANGE')toast('Fuera de alcance');else if(!r.ok&&r.reason==='COOLDOWN')toast('Ataque recargando')}
function armAbility(s){if(!state.combatEnabled||state.transitioning||!window.KeloAbilities)return;const i=window.KeloAbilities.hotbar&&window.KeloAbilities.hotbar.slots[s];if(!i)return toast('Slot vacío');const t=i.definition&&i.definition.targeting;if(t&&t.type==='self'){const r=localAuthority.execute(command('CAST_ABILITY',{request:{slotIndex:s}}));if(!r.ok)toast('No se pudo usar la habilidad');state.armedSlot=-1;return}state.armedSlot=s;toast('Habilidad lista · toca objetivo o suelo')}
function castArmedAt(w,t){const s=state.armedSlot;if(s<0||!window.KeloAbilities)return false;const i=window.KeloAbilities.hotbar&&window.KeloAbilities.hotbar.slots[s];if(!i||!i.definition){state.armedSlot=-1;return false}const d=i.definition,v={x:w.x-localPlayer.x,y:w.y-localPlayer.y},l=Math.hypot(v.x,v.y)||1,q={slotIndex:s};if(d.targeting.type==='position')q.position=w;else if(d.targeting.type==='target'){if(!t){toast('Toca un enemigo');return true}q.targetId=t.id}else if(d.targeting.type==='direction')q.direction={x:v.x/l,y:v.y/l};const r=localAuthority.execute(command('CAST_ABILITY',{request:q}));state.armedSlot=-1;if(!r.ok)toast('No se pudo usar la habilidad');return true}
function targetAt(w){const d=dummyEntity();return d&&d.hp>0&&dist(w,d)<=(d.radius||20)*2.1?d:null}function handleCombatTap(x,y){if(!state.combatEnabled||state.transitioning)return false;const w=screenWorld(x,y),t=targetAt(w);if(state.armedSlot>=0)return castArmedAt(w,t);if(!t){state.selected=null;return false}localAuthority.execute(command('SELECT_TARGET',{target:t}));present(localAuthority.execute(command('BASIC_ATTACK',{target:t})),t);return true}
function setExitVisible(v){const x=document.getElementById('lx-side-pvp-exit');if(x)x.style.display=v?'flex':'none';const y=document.getElementById('kelo-pvp-exit');if(y)y.style.display=v&&!x?'block':'none'}function ensureFallbackExit(){if(document.getElementById('lx-side-pvp-exit'))return;let e=document.getElementById('kelo-pvp-exit');if(!e){e=document.createElement('button');e.id='kelo-pvp-exit';e.type='button';e.textContent='Salir PvP';e.style.cssText='display:none;position:absolute;top:max(198px,calc(env(safe-area-inset-top) + 190px));right:max(12px,env(safe-area-inset-right));z-index:140;pointer-events:auto;background:rgba(18,20,27,.95);color:#ffd6d6;border:1px solid rgba(255,90,90,.7);border-radius:12px;padding:9px 12px;font-size:11px;font-weight:850';e.addEventListener('pointerdown',a=>{a.preventDefault();a.stopPropagation();leave()});document.body.appendChild(e)}}
function freezeMovement(){if(typeof localPlayer!=='undefined'&&localPlayer){localPlayer.vx=0;localPlayer.vy=0}if(typeof input!=='undefined'&&input){input.normX=0;input.normY=0;input.touchActive=false;input.touchId=null;if(input.keys)Object.keys(input.keys).forEach(k=>input.keys[k]=false)}}function progress(){return state.transitioning&&state.transitionStartedAt?clamp((performance.now()-state.transitionStartedAt)/TRANSITION_MS,0,1):0}function startTransition(k){state.transitioning=true;state.transitionKind=k;state.transitionStartedAt=performance.now();freezeMovement();audit(k+'-casting')}function finishTransition(){state.transitioning=false;state.transitionKind=null;state.transitionStartedAt=0}
function enter(){if(state.mode!=='social'||state.transitioning)return;if(!ready())return toast('PvP todavía cargando');const d=dummyEntity();state.saved={x:localPlayer.x,y:localPlayer.y,cameraX:camera.x,cameraY:camera.y,dummyX:d&&d.x,dummyY:d&&d.y,dummyHp:d&&d.hp,dummyMaxHp:d&&d.maxHp};state.mode='entering';state.combatEnabled=false;state.selected=null;state.armedSlot=-1;window.KELO_COMBAT_ENABLED=false;if(typeof closeMenu==='function')closeMenu();startTransition('enter');clearTimeout(state.transitionTimer);state.transitionTimer=setTimeout(()=>{try{if(!ready())throw Error('PLAYER_OR_CAMERA_UNAVAILABLE');if(d){d.x=WORLD.dummyX;d.y=WORLD.dummyY;d.hp=d.maxHp=100}localPlayer.x=WORLD.spawnX;localPlayer.y=WORLD.spawnY;localPlayer.vx=localPlayer.vy=0;camera.x=localPlayer.x;camera.y=localPlayer.y;if('targetX'in camera)camera.targetX=localPlayer.x;if('targetY'in camera)camera.targetY=localPlayer.y;document.body.classList.remove('social-mode');state.mode='pvp';state.combatEnabled=true;window.KELO_COMBAT_ENABLED=true;finishTransition();setExitVisible(true);toast('Mundo PvP');audit('entered')}catch(e){state.lastError=String(e&&e.message||e);state.mode='social';state.combatEnabled=false;window.KELO_COMBAT_ENABLED=false;finishTransition();setExitVisible(false);document.body.classList.add('social-mode');toast('No se pudo entrar al PvP');audit('enter-error');console.error('[KeloPvP] enter failed',e)}},TRANSITION_MS)}
function leave(){if(state.mode!=='pvp'||state.transitioning)return;state.mode='leaving';state.selected=null;state.armedSlot=-1;startTransition('leave');clearTimeout(state.transitionTimer);state.transitionTimer=setTimeout(()=>{try{state.combatEnabled=false;window.KELO_COMBAT_ENABLED=false;if(state.saved&&ready()){localPlayer.x=state.saved.x;localPlayer.y=state.saved.y;localPlayer.vx=localPlayer.vy=0;camera.x=state.saved.cameraX;camera.y=state.saved.cameraY;if('targetX'in camera)camera.targetX=state.saved.cameraX;if('targetY'in camera)camera.targetY=state.saved.cameraY;const d=dummyEntity();if(d&&state.saved.dummyX!=null){d.x=state.saved.dummyX;d.y=state.saved.dummyY;d.hp=state.saved.dummyHp;d.maxHp=state.saved.dummyMaxHp}}document.body.classList.add('social-mode');state.mode='social';finishTransition();setExitVisible(false);toast('Volviste al mundo social');audit('left')}catch(e){state.lastError=String(e&&e.message||e);state.combatEnabled=false;window.KELO_COMBAT_ENABLED=false;document.body.classList.add('social-mode');state.mode='social';finishTransition();setExitVisible(false);audit('leave-error');console.error('[KeloPvP] leave failed',e)}},TRANSITION_MS)}
function drawArenaScene(){if(typeof ctx==='undefined'||!ready())return;ctx.save();ctx.setTransform(1,0,0,1,0,0);ctx.fillStyle='#070b12';ctx.fillRect(0,0,canvas.width||screenW,canvas.height||screenH);ctx.restore();const z=(typeof CONFIG!=='undefined'&&CONFIG.zoom)||1;ctx.save();ctx.translate(screenW/2,screenH/2);ctx.scale(z,z);ctx.translate(-camera.x,-camera.y);ctx.fillStyle='#101722';ctx.fillRect(WORLD.x,WORLD.y,WORLD.w,WORLD.h);ctx.fillStyle='#162231';ctx.fillRect(WORLD.x+24,WORLD.y+24,WORLD.w-48,WORLD.h-48);ctx.strokeStyle='rgba(231,197,106,.72)';ctx.lineWidth=5;ctx.strokeRect(WORLD.x+18,WORLD.y+18,WORLD.w-36,WORLD.h-36);const d=dummyEntity();if(d){if(state.selected===d&&d.hp>0){ctx.strokeStyle='#ff5d5d';ctx.lineWidth=3;ctx.beginPath();ctx.ellipse(d.x,d.y+12,30,13,0,0,Math.PI*2);ctx.stroke()}if(typeof renderAvatar==='function'&&d.hp>0)renderAvatar(d,false);if(d.hp>0){ctx.fillStyle='rgba(0,0,0,.72)';ctx.fillRect(d.x-34,d.y-58,68,7);ctx.fillStyle='#ef476f';ctx.fillRect(d.x-34,d.y-58,68*clamp(d.hp/d.maxHp,0,1),7)}}if(typeof renderAvatar==='function')renderAvatar(localPlayer,true);state.floats.forEach(f=>{ctx.globalAlpha=clamp(f.life/.8,0,1);ctx.fillStyle='#fff';ctx.font='bold 16px sans-serif';ctx.textAlign='center';ctx.fillText(f.text,f.x,f.y)});ctx.globalAlpha=1;ctx.restore()}
function drawTeleportFx(){if(!state.transitioning||typeof ctx==='undefined'||!ready())return;const p=progress(),z=(typeof CONFIG!=='undefined'&&CONFIG.zoom)||1,c=p<.72?1:Math.max(.08,1-(p-.72)/.28),pulse=Math.sin(p*Math.PI*7)*.5+.5;ctx.save();ctx.translate(screenW/2,screenH/2);ctx.scale(z,z);ctx.translate(-camera.x,-camera.y);const x=localPlayer.x,y=localPlayer.y+10;ctx.globalCompositeOperation='lighter';for(let i=0;i<3;i++){const r=(26+i*13+p*22)*c;ctx.globalAlpha=(.7-i*.16)*(1-p*.3);ctx.strokeStyle=i===1?'#e7c56a':'#67c8ff';ctx.lineWidth=2.5;ctx.beginPath();ctx.ellipse(x,y,r,r*.34,0,0,Math.PI*2);ctx.stroke()}for(let i=0;i<10;i++){const a=i/10*Math.PI*2+p*Math.PI*4,rr=26+10*p+6*Math.sin(i+p*9);ctx.globalAlpha=.35+.55*pulse;ctx.fillStyle=i%2?'#8bdcff':'#ffe18a';ctx.beginPath();ctx.arc(x+Math.cos(a)*rr,y-22-p*34+Math.sin(a)*rr*.28,1.5+(i%3),0,Math.PI*2);ctx.fill()}ctx.restore()}
function audit(e){const d=dummyEntity();window.KELO_PVP_AUDIT={version:VERSION,event:e||null,mode:state.mode,combatEnabled:state.combatEnabled,transitioning:state.transitioning,transitionKind:state.transitionKind,transitionMs:TRANSITION_MS,transitionProgress:progress(),dummyAlive:!!(d&&d.hp>0),dummyHp:d?d.hp:null,armedSlot:state.armedSlot,authority:'local-command-simulation-v1',commandResultBoundary:true,isolatedArenaRender:true,fullScreenLoading:false,inWorldTeleportFx:true,lastError:state.lastError}}
const previousUpdate=typeof updateSimulation==='function'?updateSimulation:null;if(previousUpdate)updateSimulation=function(dt){previousUpdate(dt);state.basicCooldown=Math.max(0,state.basicCooldown-dt);for(let i=state.floats.length-1;i>=0;i--){state.floats[i].life-=dt;state.floats[i].y-=20*dt;if(state.floats[i].life<=0)state.floats.splice(i,1)}if(state.transitioning)freezeMovement();if(state.combatEnabled&&ready()){const r=Number(localPlayer.radius)||20;localPlayer.x=clamp(localPlayer.x,WORLD.x+r,WORLD.x+WORLD.w-r);localPlayer.y=clamp(localPlayer.y,WORLD.y+r,WORLD.y+WORLD.h-r)}audit('tick')};
const previousRender=typeof render==='function'?render:null;if(previousRender)render=function(){if(state.combatEnabled||state.mode==='leaving')drawArenaScene();else previousRender();if(state.transitioning)drawTeleportFx()};
window.addEventListener('pointerdown',function(e){if(!state.combatEnabled||state.transitioning)return;const b=e.target&&e.target.closest&&e.target.closest('.stone-slot');if(b){e.preventDefault();e.stopImmediatePropagation();armAbility(Number(b.dataset.slot));return}if(typeof canvas==='undefined'||e.target!==canvas)return;if(handleCombatTap(e.clientX,e.clientY)){e.preventDefault();e.stopImmediatePropagation()}},true);
window.enterPvPWorld=enter;window.leavePvPWorld=leave;window.KeloPvPWorld=Object.freeze({version:VERSION,enter,leave,get state(){return Object.freeze({mode:state.mode,combatEnabled:state.combatEnabled,transitioning:state.transitioning,selectedId:state.selected&&state.selected.id,armedSlot:state.armedSlot,lastError:state.lastError})},command,authority:localAuthority});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{ensureFallbackExit();setExitVisible(false);audit('boot')},{once:true});else{ensureFallbackExit();setExitVisible(false);audit('boot')}
})();