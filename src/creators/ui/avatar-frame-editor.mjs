/* KELO-INDEX
 * area: CREATORS / AVATAR / TACTILE FRAME EDITOR
 * owner: fullscreen mobile-first per-frame correction UI
 * keys: SPRITE TOUCH PINCH FOOT GHOST ERASER RESTORE PATCH UNDO REDO
 * consumes: avatar-frame-project + exact avatar-frame-compositor
 */
import {updateFramePatch,normalizeFramePatch,applyPinchFromPointers,alphaBoundsMatchProposal} from '../avatar/avatar-frame-project.mjs';
import {composeFrameCell} from '../avatar/avatar-frame-compositor.mjs';
import {brushErase,magicEraseContiguous,encodeMaskRuns,decodeMaskRuns} from '../avatar/avatar-frame-pixels.mjs';
const F=Object.freeze;
const css=`
.kfe{position:fixed;inset:0;z-index:2147483000;background:#07090d;color:#f7f4e9;font-family:Inter,system-ui,-apple-system,sans-serif;display:grid;grid-template-rows:auto 1fr auto;overscroll-behavior:none}.kfe *{box-sizing:border-box}.kfe-head{display:flex;align-items:center;gap:8px;padding:max(10px,env(safe-area-inset-top)) 10px 8px;border-bottom:1px solid #272c35;background:#0d1016}.kfe-head b{font-size:13px;letter-spacing:.06em}.kfe-head span{color:#9ca2ad;font-size:11px}.kfe-btn{border:1px solid #343b47;background:#151a22;color:#f8f8f8;border-radius:12px;min-height:42px;padding:8px 11px;font-weight:850;font-size:11px}.kfe-btn.gold{background:#d1ae58;color:#151109;border-color:#d1ae58}.kfe-btn.on{border-color:#d1ae58;color:#f1d579;background:#211c10}.kfe-close{margin-left:auto}.kfe-stage{position:relative;min-height:0;display:grid;place-items:center;overflow:hidden;background-color:#0a0d12;background-image:linear-gradient(45deg,#121720 25%,transparent 25%),linear-gradient(-45deg,#121720 25%,transparent 25%),linear-gradient(45deg,transparent 75%,#121720 75%),linear-gradient(-45deg,transparent 75%,#121720 75%);background-size:20px 20px;background-position:0 0,0 10px,10px -10px,-10px 0}.kfe-canvas{width:min(100vw,620px);height:min(67vh,620px);touch-action:none;display:block}.kfe-status{position:absolute;left:10px;top:10px;right:10px;display:flex;justify-content:space-between;pointer-events:none;font-size:10px;color:#b5bac4}.kfe-bottom{background:#0d1016;border-top:1px solid #272c35;padding:8px 8px max(9px,env(safe-area-inset-bottom));display:grid;gap:7px}.kfe-tools{display:flex;gap:6px;overflow:auto;padding-bottom:2px}.kfe-tools .kfe-btn{min-width:70px;flex:0 0 auto}.kfe-actions{display:flex;gap:6px}.kfe-actions .kfe-btn{flex:1}.kfe-range{display:flex;align-items:center;gap:8px;color:#9ca2ad;font-size:10px}.kfe-range input{flex:1}.kfe-pop{position:fixed;inset:0;z-index:2147483010;background:#080b10eF;display:grid;grid-template-rows:auto 1fr auto;padding:12px}.kfe-pop canvas{width:100%;height:100%;touch-action:none;object-fit:contain}.kfe-pop p{font-size:11px;color:#aeb4bf}.kfe-hidden{display:none}
@media(min-width:800px){.kfe{grid-template-columns:1fr 340px;grid-template-rows:auto 1fr}.kfe-head{grid-column:1/-1}.kfe-bottom{grid-column:2;grid-row:2;align-content:start}.kfe-stage{grid-column:1;grid-row:2}.kfe-tools{flex-wrap:wrap}.kfe-tools .kfe-btn{min-width:100px}}
`;
const node=(d,t,p={},kids=[])=>{const e=d.createElement(t);for(const[k,v]of Object.entries(p)){if(k==='class')e.className=v;else if(k==='text')e.textContent=v;else e[k]=v;}for(const x of [].concat(kids||[]))if(x)e.append(x);return e;};
const clone=v=>typeof structuredClone==='function'?structuredClone(v):JSON.parse(JSON.stringify(v));
const point=(event,canvas)=>{const r=canvas.getBoundingClientRect();return{x:(event.clientX-r.left)*canvas.width/Math.max(1,r.width),y:(event.clientY-r.top)*canvas.height/Math.max(1,r.height)};};

export async function openAvatarFrameEditor({root=globalThis,project,index,store,onCommit=null}={}){
  if(!root.document||!project?.slots?.[index]||!store)throw new Error('FRAME_EDITOR_ARGUMENTS_REQUIRED');
  const d=root.document,style=node(d,'style',{textContent:css}),shell=node(d,'section',{class:'kfe'}),head=node(d,'header',{class:'kfe-head'}),title=node(d,'b',{text:`FRAME ${index+1}/16 · ${project.slots[index].directionKey.toUpperCase()} · ${project.slots[index].phase+1}`}),hint=node(d,'span',{text:'pies anclados'}),close=node(d,'button',{class:'kfe-btn kfe-close',text:'CERRAR'});head.append(title,hint,close);
  const stage=node(d,'main',{class:'kfe-stage'}),canvas=node(d,'canvas',{class:'kfe-canvas'});canvas.width=620;canvas.height=620;const status=node(d,'div',{class:'kfe-status'});stage.append(canvas,status);
  const bottom=node(d,'footer',{class:'kfe-bottom'}),tools=node(d,'div',{class:'kfe-tools'}),actions=node(d,'div',{class:'kfe-actions'}),range=node(d,'div',{class:'kfe-range'});bottom.append(tools,range,actions);shell.append(head,stage,bottom);d.head.append(style);d.body.append(shell);
  const sourceResolver=key=>store.getSource(key);let working=clone(project),slot=working.slots[index],patch=normalizeFramePatch(slot.patch||{}),tool='move',ghost=.5,ghostOn=false,flicker=false,frame=null,reference=null,raf=0,dirty=true,history=[],future=[],gesture=null,mask=null,maskW=0,maskH=0,magicTolerance=34,brushRadius=8,autoProposal=null;
  const toolButtons=new Map();
  const addTool=(key,label)=>{const b=node(d,'button',{class:'kfe-btn',text:label});b.onclick=()=>{tool=key;for(const[x,n]of toolButtons)n.classList.toggle('on',x===tool);};toolButtons.set(key,b);tools.append(b);return b;};
  addTool('move','✋ MOVE');addTool('scale','↕ SCALE');addTool('erase','⌫ ERASE');addTool('magic','✨ MAGIC');addTool('restore','↩ RESTORE');
  const ghostBtn=addTool('ghost','◫ GHOST');ghostBtn.onclick=()=>{ghostOn=!ghostOn;ghostBtn.classList.toggle('on',ghostOn);dirty=true;};
  const patchBtn=addTool('patch','+ PATCH');
  const autoBtn=node(d,'button',{class:'kfe-btn',text:'AUTO MATCH'});tools.append(autoBtn);
  const undoBtn=node(d,'button',{class:'kfe-btn',text:'UNDO'}),redoBtn=node(d,'button',{class:'kfe-btn',text:'REDO'}),resetBtn=node(d,'button',{class:'kfe-btn',text:'RESET'});tools.append(undoBtn,redoBtn,resetBtn);
  const opacity=node(d,'input',{type:'range',min:0,max:75,step:25,value:50});range.append(node(d,'span',{text:'GHOST'}),opacity);opacity.oninput=()=>{ghost=Number(opacity.value)/100;dirty=true;};
  const minus=node(d,'button',{class:'kfe-btn',text:'−'}),plus=node(d,'button',{class:'kfe-btn',text:'+'}),flick=node(d,'button',{class:'kfe-btn',text:'FLICKER'}),save=node(d,'button',{class:'kfe-btn gold',text:'GUARDAR'});actions.append(minus,plus,flick,save);
  const syncSlot=()=>{working=updateFramePatch(working,index,patch,{state:'NEEDS_REVIEW'});slot=working.slots[index];};
  const pushHistory=label=>{history.push({patch:clone(patch),label});if(history.length>80)history.shift();future=[];};
  const setPatch=(next,{record=true,label='edit'}={})=>{if(record)pushHistory(label);patch=normalizeFramePatch(next);syncSlot();dirty=true;};
  async function ensureMask(){if(mask)return;const record=await sourceResolver(slot.sourceKey);if(!record?.blob)return;const bitmap=root.createImageBitmap?await root.createImageBitmap(record.blob):null;if(bitmap){maskW=bitmap.width;maskH=bitmap.height;bitmap.close?.();}else{maskW=slot.sourceWidth;maskH=slot.sourceHeight;}const rect=patch.sourceRect||{x:0,y:0,w:maskW,h:maskH};maskW=Math.max(1,Math.round(rect.w??rect.width));maskH=Math.max(1,Math.round(rect.h??rect.height));mask=patch.eraseMask?decodeMaskRuns(patch.eraseMask,maskW,maskH):new Uint8Array(maskW*maskH);}
  function commitMask(next,label){pushHistory(label);mask=next;patch=normalizeFramePatch({...patch,eraseMask:encodeMaskRuns(mask)});syncSlot();dirty=true;}
  async function render(){
    if(!dirty)return;dirty=false;frame=await composeFrameCell(root,sourceResolver,{...slot,patch},{cellWidth:canvas.width,cellHeight:canvas.height,padding:54});const ctx=canvas.getContext('2d');ctx.clearRect(0,0,canvas.width,canvas.height);
    if(ghostOn){let ri=Number.isInteger(project.referenceFrameIndex)?project.referenceFrameIndex:working.slots.findIndex((s,i)=>i!==index&&s.sourceKey);if(ri>=0){reference=await composeFrameCell(root,sourceResolver,working.slots[ri],{cellWidth:canvas.width,cellHeight:canvas.height,padding:54});const show=!flicker||Math.floor(Date.now()/180)%2===0;if(show){ctx.save();ctx.globalAlpha=ghost;ctx.drawImage(reference.canvas,0,0);ctx.restore();}}}
    ctx.drawImage(frame.canvas,0,0);ctx.save();ctx.strokeStyle='rgba(209,174,88,.65)';ctx.setLineDash([7,7]);ctx.beginPath();ctx.moveTo(0,canvas.height-54+patch.y);ctx.lineTo(canvas.width,canvas.height-54+patch.y);ctx.stroke();ctx.strokeStyle='rgba(120,180,255,.45)';ctx.beginPath();ctx.moveTo(canvas.width/2+patch.x,0);ctx.lineTo(canvas.width/2+patch.x,canvas.height);ctx.stroke();ctx.restore();status.replaceChildren(node(d,'span',{text:`${tool.toUpperCase()} · scale ${patch.scale.toFixed(2)} · x ${Math.round(patch.x)} · y ${Math.round(patch.y)}`}),node(d,'span',{text:ghostOn?'GHOST ON':'GHOST OFF'}));
  }
  async function loop(){try{if(flicker&&ghostOn)dirty=true;await render();}catch{}raf=root.requestAnimationFrame(loop);}raf=root.requestAnimationFrame(loop);
  const pointers=new Map();
  canvas.onpointerdown=async event=>{canvas.setPointerCapture?.(event.pointerId);const p=point(event,canvas);pointers.set(event.pointerId,p);if(tool==='erase'||tool==='restore'||tool==='magic'){await ensureMask();if(!frame?.placement||!mask)return;const pl=frame.placement,rect=pl.sourceRect,localX=(p.x-pl.x)/Math.max(1,pl.width)*rect.w,localY=(p.y-pl.y)/Math.max(1,pl.height)*rect.h;if(tool==='magic'){const record=await sourceResolver(slot.sourceKey),bitmap=await (root.createImageBitmap?.(record.blob));if(bitmap){const temp=d.createElement('canvas');temp.width=bitmap.width;temp.height=bitmap.height;const tc=temp.getContext('2d',{willReadFrequently:true});tc.drawImage(bitmap,0,0);const pixels=tc.getImageData(rect.x,rect.y,rect.w,rect.h).data;bitmap.close?.();const result=magicEraseContiguous(pixels,rect.w,rect.h,localX,localY,{tolerance:magicTolerance,mask});commitMask(result.mask,'magic erase');}}else{gesture={kind:'paint',before:clone(patch),maskBefore:new Uint8Array(mask)};const result=brushErase(mask,maskW,maskH,localX,localY,{radius:brushRadius,restore:tool==='restore'});mask=result.mask;patch=normalizeFramePatch({...patch,eraseMask:encodeMaskRuns(mask)});syncSlot();dirty=true;}return;}
    gesture={kind:'transform',before:clone(patch),start:new Map(pointers)};
  };
  canvas.onpointermove=async event=>{if(!pointers.has(event.pointerId))return;const p=point(event,canvas);pointers.set(event.pointerId,p);if(gesture?.kind==='paint'&&(tool==='erase'||tool==='restore')){if(!frame?.placement||!mask)return;const pl=frame.placement,rect=pl.sourceRect,localX=(p.x-pl.x)/Math.max(1,pl.width)*rect.w,localY=(p.y-pl.y)/Math.max(1,pl.height)*rect.h;mask=brushErase(mask,maskW,maskH,localX,localY,{radius:brushRadius,restore:tool==='restore'}).mask;patch=normalizeFramePatch({...patch,eraseMask:encodeMaskRuns(mask)});syncSlot();dirty=true;return;}
    if(gesture?.kind!=='transform')return;const ids=[...pointers.keys()];if(ids.length>=2){const aId=ids[0],bId=ids[1],a0=gesture.start.get(aId)||pointers.get(aId),b0=gesture.start.get(bId)||pointers.get(bId);patch=normalizeFramePatch(applyPinchFromPointers(gesture.before,{a0,b0,a1:pointers.get(aId),b1:pointers.get(bId)}));}else{const id=ids[0],start=gesture.start.get(id)||pointers.get(id),now=pointers.get(id);if(tool==='scale'){const factor=Math.exp((start.y-now.y)/220);patch=normalizeFramePatch({...gesture.before,scale:gesture.before.scale*factor});}else patch=normalizeFramePatch({...gesture.before,x:gesture.before.x+(now.x-start.x),y:gesture.before.y+(now.y-start.y)});}syncSlot();dirty=true;};
  const endPointer=event=>{if(!pointers.has(event.pointerId))return;pointers.delete(event.pointerId);if(pointers.size)return;if(gesture?.kind==='paint'){history.push({patch:gesture.before,label:tool});future=[];}else if(gesture?.kind==='transform'){history.push({patch:gesture.before,label:'gesture'});future=[];}gesture=null;};canvas.onpointerup=endPointer;canvas.onpointercancel=endPointer;
  canvas.onwheel=event=>{event.preventDefault();setPatch({...patch,scale:patch.scale*Math.exp(-event.deltaY*.0015)},{label:'wheel scale'});};
  minus.onclick=()=>setPatch({...patch,scale:patch.scale*.94},{label:'scale down'});plus.onclick=()=>setPatch({...patch,scale:patch.scale*1.06},{label:'scale up'});flick.onclick=()=>{flicker=!flicker;flick.classList.toggle('on',flicker);dirty=true;};
  undoBtn.onclick=()=>{if(!history.length)return;future.push({patch:clone(patch)});patch=normalizeFramePatch(history.pop().patch);mask=null;syncSlot();dirty=true;};redoBtn.onclick=()=>{if(!future.length)return;history.push({patch:clone(patch)});patch=normalizeFramePatch(future.pop().patch);mask=null;syncSlot();dirty=true;};resetBtn.onclick=()=>setPatch({scale:1,x:0,y:0,patchLayers:[],eraseMask:null,referenceFrameIndex:null},{label:'reset'});
  autoBtn.onclick=async()=>{if(!frame?.bounds)return;let ri=Number.isInteger(project.referenceFrameIndex)?project.referenceFrameIndex:working.slots.findIndex((s,i)=>i!==index&&s.sourceKey);if(ri<0)return;reference=await composeFrameCell(root,sourceResolver,working.slots[ri],{cellWidth:canvas.width,cellHeight:canvas.height,padding:54});if(!reference.bounds)return;autoProposal=alphaBoundsMatchProposal(frame.bounds,reference.bounds,patch);const accept=root.confirm?.(`AUTO MATCH propone escala ${autoProposal.scale.toFixed(2)}. ¿Aplicar?`);if(accept)setPatch(autoProposal,{label:'auto match'});};
  async function addPatchFromOriginal(){const record=await sourceResolver(slot.sourceKey);if(!record?.blob)return;const bitmap=await root.createImageBitmap?.(record.blob);if(!bitmap)return;const pop=node(d,'div',{class:'kfe-pop'}),ph=node(d,'header',{},[node(d,'b',{text:'PATCH DESDE ORIGINAL'}),node(d,'p',{text:'Arrastra un rectángulo sobre la parte que quieres recuperar.'})]),pc=node(d,'canvas');pc.width=bitmap.width;pc.height=bitmap.height;const pg=pc.getContext('2d');pg.drawImage(bitmap,0,0);bitmap.close?.();const cancel=node(d,'button',{class:'kfe-btn',text:'CANCELAR'}),accept=node(d,'button',{class:'kfe-btn gold',text:'AÑADIR PATCH',disabled:true}),foot=node(d,'footer',{class:'kfe-actions'},[cancel,accept]);pop.append(ph,pc,foot);d.body.append(pop);let start=null,selection=null;const redraw=async()=>{const b=await root.createImageBitmap(record.blob);pg.clearRect(0,0,pc.width,pc.height);pg.drawImage(b,0,0);b.close?.();if(selection){pg.strokeStyle='#d1ae58';pg.lineWidth=Math.max(1,pc.width/300);pg.strokeRect(selection.x,selection.y,selection.w,selection.h);}};pc.onpointerdown=e=>{start=point(e,pc);};pc.onpointermove=e=>{if(!start)return;const q=point(e,pc);selection={x:Math.round(Math.min(start.x,q.x)),y:Math.round(Math.min(start.y,q.y)),w:Math.max(1,Math.round(Math.abs(q.x-start.x))),h:Math.max(1,Math.round(Math.abs(q.y-start.y)))};accept.disabled=false;void redraw();};pc.onpointerup=()=>{start=null;};cancel.onclick=()=>pop.remove();accept.onclick=()=>{if(!selection)return;setPatch({...patch,patchLayers:[...(patch.patchLayers||[]),{sourceKey:slot.sourceKey,sourceRect:selection,x:0,y:-54,scale:1,eraseMask:null,visible:true}]},{label:'add patch'});pop.remove();};}
  patchBtn.onclick=()=>void addPatchFromOriginal();
  save.onclick=async()=>{syncSlot();working.slots[index].state=frame?.bounds?'VALID':'ERROR';working.updatedAt=Date.now();await store.saveProject(working);await onCommit?.(working,index);destroy();};
  function destroy(){root.cancelAnimationFrame(raf);shell.remove();style.remove();}
  close.onclick=destroy;
  toolButtons.get('move')?.classList.add('on');
  return F({shell,close:destroy,getProject:()=>clone(working),getPatch:()=>clone(patch)});
}
